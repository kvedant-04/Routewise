---
phase: 11
plan: 2
wave: 2
depends_on: [11.1]
---

# Plan 11.2: Frontend Zero-Trust Mapping

<objective>
Implement a "Zero-Trust" data mapping layer in `App.jsx` to ensure the UI remains resilient and deterministic even if the backend returns partial or malformed data.

Purpose: Eliminate React crashes and "undefined" text in the UI.
Output: Updated `frontend/src/App.jsx`.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- frontend/src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Implement Bulletproof safeEvents Mapping</name>
  <files>frontend/src/App.jsx</files>
  <action>
    Replace the existing `safeEvents` `useMemo` with the requested zero-trust implementation:
    - Default fields for every activity (activity, place, cost, notes).
    - Handle both `time_slot` and legacy `time` fields.
    - Provide fallback values ("Untitled Activity", "Unknown Location", etc.) for missing data.
    - Assert default `duration_mins` is an integer.
  </action>
  <verify>Check the console logs for "DEBUG: PARSED SAFE EVENTS" to ensure no undefined values exist.</verify>
  <done>safeEvents returns a clean array of consistent objects regardless of input jitter.</done>
</task>

<task type="auto">
  <name>Add Render Crash Guards & Error States</name>
  <files>frontend/src/App.jsx</files>
  <action>
    In `handlePlanTrip`, add validation before calling `setItinerary`.
    If `parsed.days` is missing or not an array, trigger `setError` and block the render.
    Ensure components like `ListView` have internal checks for `item.activity` etc.
  </action>
  <verify>Manually simulate an empty "days" array from the API and verify the error message appears without crashing.</verify>
  <done>Frontend displays a user-friendly error instead of a white screen on structural failure.</done>
</task>

<task type="auto">
  <name>Remove Legacy Field Dependencies & Regex</name>
  <files>frontend/src/App.jsx</files>
  <action>
    Scan for and delete any remaining references to `description` or old regex parsing logic that might conflict with the new JSON-only pipeline.
    Standardize on `start_time` and `duration_mins` globally.
  </action>
  <verify>Grep codebase for "description" to ensure zero functional references remain in the itinerary path.</verify>
  <done>Codebase is clean of legacy debt relating to the previous unstructured model.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] UI displays fallbacks like "Untitled Activity" rather than empty strings.
- [ ] No React key errors or rendering crashes occur with partial data.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] UI is crash-proof across all views (List, Timeline, Calendar)
</success_criteria>

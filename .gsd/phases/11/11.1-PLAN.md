---
phase: 11
plan: 1
wave: 1
---

# Plan 11.1: Backend Schema & Normalization

<objective>
Harden the backend `main.py` to ensure it ALWAYS returns a perfect, schema-valid JSON object by implementing a strict normalization layer and a deterministic hard fallback.

Purpose: Prevent frontend crashes caused by missing or renamed AI fields.
Output: Updated `backend/main.py`.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Enforce Strict Pydantic Schema & Prompting</name>
  <files>backend/main.py</files>
  <action>
    Update `Activity` and `ItineraryJSON` models to match the requested structure exactly.
    Refine the `prompt` in `plan_trip` to use absolute field names (start_time, duration_mins) and explicit "NO MARKDOWN" rules.
    AVOID: Relying on AI to naturally handle aliases; use Pydantic `robust_mapping` for transformation.
  </action>
  <verify>Run the backend and check if Pydantic yields the correct schema on startup.</verify>
  <done>Models in main.py mirror the SPEC perfectly.</done>
</task>

<task type="auto">
  <name>Implement Normalization Layer & Validation</name>
  <files>backend/main.py</files>
  <action>
    Inside `plan_trip`, after `extract_json`, implement a normalization loop that:
    - Aliases `time` -> `start_time`
    - Converts `duration` strings to `duration_mins` (integers)
    - Replaces "City Center" or generic placeholders with meaningful text or the destination name.
    Tighten `is_valid` to reject any plan failing the daily activity count or required key presence.
  </action>
  <verify>Manually trigger a response with "time" field and verify it becomes "start_time" in the API response.</verify>
  <done>Backend cleans and validates AI output before it ever reaches the return statement.</done>
</task>

<task type="auto">
  <name>Deterministically Map Hard Fallback</name>
  <files>backend/main.py</files>
  <action>
    Update the `MAX_RETRIES` failure block to return a hardcoded, perfectly valid `ItineraryJSON` object.
    Ensure this fallback uses the new `Activity` model structure.
  </action>
  <verify>Temporarily break the AI call and verify the fallback JSON is returned correctly.</verify>
  <done>Backend never returns success:False or malformed JSON, even on total AI failure.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Backend returns valid JSON for all request types.
- [ ] Field names are consistent (start_time, duration_mins).
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Backend response matches Success Criteria in SPEC.md
</success_criteria>

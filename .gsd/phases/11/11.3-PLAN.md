---
phase: 11
plan: 3
wave: 3
depends_on: [11.2]
---

# Plan 11.3: Map Safety & Component Resilience

<objective>
Finalize the stability layer by hardening the map's geocoding filters and ensuring all sub-components (ListView, etc.) are resilient to the new "Zero-Trust" data structure.

Purpose: Complete the production-grade reliability across all UI layers.
Output: Updated `MapIntelligence.jsx`, `ListView.jsx`, `TimelineView.jsx`, `CalendarView.jsx`.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- frontend/src/components/MapIntelligence.jsx
- frontend/src/components/ListView.jsx
- frontend/src/components/TimelineView.jsx
- frontend/src/components/CalendarView.jsx
</context>

<tasks>

<task type="auto">
  <name>Harden Map Geocoding Filters & Popups</name>
  <files>frontend/src/components/MapIntelligence.jsx</files>
  <action>
    Update `validPoints` filter to strictly require `p.lat && p.lng`. 
    Update Popup bindings to use the new activity field structure (activity, place, start_time, notes, cost).
    Implement fallback strings for Popup fields directly.
    AVOID: Binding `undefined` or null to Popup components.
  </action>
  <verify>Open the map and verify all activity popups show correct data without "undefined" anywhere.</verify>
  <done>Map popups are rich, accurate, and stable.</done>
</task>

<task type="auto">
  <name>Defensive Components Verification</name>
  <files>
    frontend/src/components/ListView.jsx
    frontend/src/components/TimelineView.jsx
    frontend/src/components/CalendarView.jsx
  </files>
  <action>
    Review and update each view component to use the normalized `safeEvents` fields.
    Add logical OR fallbacks where missing for extra safety.
    Standardize on `item.exactTime` and `item.duration`.
  </action>
  <verify>Switch between List, Timeline, and Calendar views for a 3-day itinerary and confirm data is identical and complete across all.</verify>
  <done>All UI views are synchronized and resilient.</done>
</task>

<task type="auto">
  <name>Final Phase 11 Stress Test & Cleanup</name>
  <files>
    backend/main.py
    frontend/src/App.jsx
  </files>
  <action>
    Perform a series of "Chaos Tests" (e.g., forcing AI to return "time" instead of "time_slot") and verify normalization handles it.
    Verify the "Hard Fallback" object renders perfectly on all views.
    Check for console errors during the entire flows.
  </action>
  <verify>Console is free of React key warnings and undefined data logs.</verify>
  <done>System is verified production-ready and deterministic.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Map center is accurate.
- [ ] No component crash on view switching.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] End-to-end data pipeline is bulletproof
</success_criteria>

---
phase: 13
plan: 1
wave: 1
---

# Plan 13.1: Image Relevance Engine

## Objective
Fix semantic mapping of imagery to ensure high-accuracy visual representation of itinerary activities. Eliminate completely unassociated fallback imagery instances which destroy product trust.

## Context
- .gsd/SPEC.md
- frontend/src/utils/mediaEngine.js

## Tasks

<task type="auto">
  <name>Implement Semantic Query Mapper</name>
  <files>frontend/src/utils/mediaEngine.js</files>
  <action>
    Modify the Unsplash/Picsum image fetching query builder.
    - DO NOT use purely generic queries.
    - Parse `evt.place`, `evt.activity`, and `destination`.
    - Construct an explicit query string strictly formatting `cleanPlace` + `destination` + `activity`.
    - Add strict fallback to safe `destination landmarks` rather than purely random "travel" fallbacks.
  </action>
  <verify>npm run dev</verify>
  <done>Images appearing in itinerary cards correctly map to the subject/nature of the actual activity described in 100% of tested components.</done>
</task>

## Success Criteria
- [ ] Query structure dynamically prioritizes the specific place/activity context above random placeholders.
- [ ] No layout shift induced by the logical rewriting of the image fetching helper function.

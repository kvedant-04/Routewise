---
phase: 13
plan: 4
wave: 1
---

# Plan 13.4: Metadata Badge Integration

## Objective
Anchor floating metadata (time, duration, cost) logically into the layout rhythm and container hierarchy so it forms a cohesive control array.

## Context
- frontend/src/components/ListView.jsx
- frontend/src/media.css

## Tasks

<task type="auto">
  <name>Redesign Badge Geometry within UI Flow</name>
  <files>
    frontend/src/components/ListView.jsx
    frontend/src/media.css
  </files>
  <action>
    - Convert arbitrary floating `.lv-badges` strings into structured Pill/Badge styled elements perfectly aligned along the card flow (either inline with the title or bottom flush with place).
    - Use strict color tokenization matching `Stitch AI` standard badge appearances.
  </action>
  <verify>npm run dev</verify>
  <done>Duration and Cost badges sit naturally in a cohesive container, styled clearly as distinct tags, not loose floating blocks.</done>
</task>

## Success Criteria
- [ ] Badges have distinct, aesthetically pleasing visual boundaries (borders or background tints).
- [ ] Spacing between badge nodes is mathematically uniform.
